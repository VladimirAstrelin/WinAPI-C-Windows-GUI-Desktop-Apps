// ================================================================
// dialog_proc.h – заголовочный файл, в котором объявлена процедура обработки диалога.
// Его включают в main.c и в dialog_proc.c, чтобы компилятор знал сигнатуру функции.
// ================================================================

#pragma once                      // Защита от повторного включения (аналог #ifndef ... #endif)

#include <windows.h>              // Нужен для типов HWND, UINT, WPARAM, LPARAM и др.

// Объявление функции, которая будет обрабатывать все сообщения диалога.
// Возвращает INT_PTR (целое, которое может быть указателем), обычно TRUE или FALSE.
// Параметры:
//   hwnd   – дескриптор окна диалога
//   uMsg   – идентификатор сообщения (например, WM_INITDIALOG, WM_COMMAND, WM_SIZE)
//   wParam – первый параметр сообщения (зависит от uMsg)
//   lParam – второй параметр сообщения (зависит от uMsg)
INT_PTR CALLBACK EditDemoProc(HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM lParam);