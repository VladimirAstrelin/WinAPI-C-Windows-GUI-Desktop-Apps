// ================================================================
// main.c – точка входа в приложение.
// Здесь мы просто создаём диалоговое окно и запускаем цикл обработки сообщений.
// ================================================================

#include <windows.h>      // Основной заголовок Windows API – все типы, константы, функции
#include "resource.h"     // Заголовок с идентификаторами ресурсов (ID диалога, контролов)
#include "dialog_proc.h"  // Прототип нашей диалоговой процедуры (чтобы компилятор знал о ней)

// WinMain – стандартная точка входа для любого Windows-приложения.
// Параметры:
//   hInstance      – дескриптор экземпляра приложения (используется для загрузки ресурсов)
//   hPrevInstance  – всегда NULL в современных версиях Windows, оставлен для совместимости
//   lpCmdLine      – командная строка (в нашем случае не используется)
//   nCmdShow       – как показывать окно (для диалога игнорируется, он показывается модально)
int WINAPI WinMain(_In_ HINSTANCE hInstance, _In_opt_ HINSTANCE hPrevInstance,
    _In_ LPSTR lpCmdLine, _In_ int nCmdShow)
{
    // DialogBox – создаёт модальное диалоговое окно.
    // Модальное означает, что пока оно открыто, пользователь не может взаимодействовать
    // с другими окнами этого приложения (если они есть).
    // Функция возвращает управление только после того, как диалог будет закрыт (через EndDialog).
    //
    // Параметры:
    //   hInstance            – дескриптор текущего приложения
    //   MAKEINTRESOURCE(IDD_EDIT_DEMO) – преобразует числовой ID диалога в строку-ресурс
    //   NULL                 – родительское окно (нет родителя)
    //   EditDemoProc         – указатель на функцию, которая будет обрабатывать сообщения диалога
    DialogBox(hInstance, MAKEINTRESOURCE(IDD_EDIT_DEMO), NULL, EditDemoProc);

    // После закрытия диалога возвращаем 0 – это сигнал системе, что приложение завершилось успешно.
    return 0;
}