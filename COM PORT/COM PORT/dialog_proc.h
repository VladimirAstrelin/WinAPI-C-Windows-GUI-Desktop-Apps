// ============================================================
// dialog_proc.h
// Заголовочный файл, в котором объявляются (прототипируются)
// функции, используемые для работы с диалоговым окном.
// Это позволяет другим файлам (например, main.c) знать о
// существовании этих функций.
// ============================================================

// #pragma once – директива препроцессора, которая гарантирует,
// что данный заголовочный файл будет включён только один раз
// за компиляцию (альтернатива #ifndef).
#pragma once

// Объявление процедуры диалогового окна.
// Это функция, которая обрабатывает все сообщения, приходящие
// к нашему окну (например, нажатия кнопок, закрытие окна и т.д.).
// INT_PTR – целочисленный тип, возвращаемый функцией (обычно TRUE/FALSE).
// CALLBACK – соглашение о вызове (используется Windows для callback-функций).
// Параметры:
//   HWND hDlg   – дескриптор (handle) нашего диалогового окна.
//   UINT uMsg   – код сообщения (например, WM_INITDIALOG, WM_COMMAND).
//   WPARAM wParam, LPARAM lParam – дополнительные параметры сообщения.
INT_PTR CALLBACK MainDialogProc(HWND hDlg, UINT uMsg, WPARAM wParam, LPARAM lParam);

// Объявление функции, которая заполняет выпадающий список
// COM-портами из системного реестра.
// Параметр:
//   HWND hCombo – дескриптор элемента ComboBox, который нужно заполнить.
void PopulateComPorts(HWND hCombo);