// =============================================================================
// main.c — Точка входа программы
//
// Здесь всё начинается. WinMain — это аналог main() для GUI-приложений
// Windows. Консольные программы используют main(), GUI-программы — WinMain().
// Компоновщик (linker) ищет именно эту функцию как точку входа.
// =============================================================================

// Главный заголовок WinAPI — подключает всё необходимое для работы с Windows
#include <windows.h>

// Константы ресурсов, определённых в .rc файле:
//   IDD_MAIN_DIALOG — числовой ID нашего диалогового окна
//   IDC_BTN_REFRESH — числовой ID кнопки "Refresh"
//   ID_LOG          — числовой ID Edit-контрола
#include "resource.h"

// Объявление MainDialogProc (реализация в dialog_proc.c)
#include "dialog_proc.h"

// =============================================================================
// ФУНКЦИЯ: WinMain — точка входа GUI-приложения Windows
//
// Параметры (Windows передаёт их автоматически при запуске программы):
//   hInstance     — дескриптор текущего экземпляра приложения.
//                   Нужен для загрузки ресурсов (диалогов, иконок и т.д.)
//   hPrevInstance — всегда NULL в современных Windows (наследие Win16).
//                   Можно игнорировать.
//   lpCmdLine     — командная строка в виде ANSI-строки (аргументы запуска).
//                   Если нужны Unicode-аргументы — используйте GetCommandLineW().
//   nCmdShow      — как показать главное окно (SW_SHOW, SW_MINIMIZE и т.д.).
//                   Для диалогов обычно не используется.
//
// Аннотации _In_ и _In_opt_:
//   Это SAL-аннотации (Microsoft Source Annotation Language) — подсказки
//   статическому анализатору кода. _In_ = обязательный входной параметр,
//   _In_opt_ = необязательный (может быть NULL). На работу не влияют.
// =============================================================================
int WINAPI WinMain(
    _In_     HINSTANCE hInstance,
    _In_opt_ HINSTANCE hPrevInstance,
    _In_     LPSTR     lpCmdLine,
    _In_     int       nCmdShow)
{
    // -------------------------------------------------------------------------
    // DialogBox — создаёт и отображает МОДАЛЬНЫЙ диалог.
    //
    // "Модальный" означает: пока диалог открыт, пользователь не может
    // взаимодействовать с другими окнами нашего приложения. Функция
    // БЛОКИРУЕТСЯ и не возвращает управление до тех пор, пока диалог
    // не будет закрыт (через EndDialog).
    //
    // Параметры:
    //   hInstance              — наш экземпляр приложения (для поиска ресурса)
    //   MAKEINTRESOURCE(IDD_MAIN_DIALOG) — преобразует числовой ID ресурса
    //                            в LPCTSTR, понятный Windows.
    //                            IDD_MAIN_DIALOG определён в resource.h.
    //   NULL                   — родительское окно: NULL = рабочий стол
    //   MainDialogProc         — указатель на нашу оконную процедуру.
    //                            Windows будет вызывать её для каждого
    //                            сообщения, которое приходит диалогу.
    //
    // Возвращает: значение, переданное в EndDialog() при закрытии диалога
    // (IDOK или IDCANCEL). Мы его не используем.
    // -------------------------------------------------------------------------
    INT_PTR result = DialogBox(
        hInstance,
        MAKEINTRESOURCE(IDD_MAIN_DIALOG),
        NULL,
        MainDialogProc
    );

    // Возвращаем 0 — стандартный код успешного завершения приложения.
    // Windows использует это значение как exit code процесса.
    return 0;
}
