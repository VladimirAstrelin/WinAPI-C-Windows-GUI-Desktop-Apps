// main.c – точка входа в Windows-приложение.
// Здесь создаётся диалоговое окно и запускается цикл обработки сообщений.
// Этот файл минимален: он только инициализирует диалог и передаёт управление процедуре диалога.

#include <windows.h>      // Основной заголовок Windows API (типы, константы, функции)
#include "resource.h"     // Заголовок с идентификаторами ресурсов (диалогов, кнопок и т.д.)
#include "dialog_proc.h"  // Прототип нашей диалоговой процедуры (чтобы компилятор знал о ней)

// WinMain – стандартная точка входа для Windows-приложений.
// Параметры:
//   hInstance      – дескриптор экземпляра приложения (используется для загрузки ресурсов)
//   hPrevInstance  – всегда NULL в современных Windows (для совместимости)
//   lpCmdLine      – командная строка (ANSI, но мы не используем)
//   nCmdShow       – как показывать окно (но для диалога это игнорируется)
int WINAPI WinMain(_In_ HINSTANCE hInstance, _In_opt_ HINSTANCE hPrevInstance,
    _In_ LPSTR lpCmdLine, _In_ int nCmdShow)
{
    // DialogBox – создаёт модальное диалоговое окно и не возвращает управление,
    // пока окно не будет закрыто (EndDialog).
    // Параметры:
    //   hInstance           – текущий экземпляр приложения
    //   MAKEINTRESOURCE(IDD_MAIN_DIALOG) – преобразует числовой ID диалога в строковый ресурс
    //   NULL                – родительское окно (нет родителя)
    //   MainDialogProc      – указатель на функцию, которая обрабатывает все сообщения диалога
    DialogBox(hInstance, MAKEINTRESOURCE(IDD_MAIN_DIALOG), NULL, MainDialogProc);

    // Когда DialogBox завершается (пользователь закрыл окно), возвращаем 0.
    return 0;
}