// ================================================================
// dialog_proc.h – объявление функции-обработчика диалога.
// Это нужно, чтобы main.c знал о существовании DlgProc.
// ================================================================

#ifndef DIALOG_PROC_H
#define DIALOG_PROC_H

// Прототип оконной процедуры для немодального диалога.
// _In_ – аннотация SAL, говорит, что параметр является входным.
// CALLBACK – соглашение о вызове для callback-функций Windows.
INT_PTR CALLBACK DlgProc(
    _In_ HWND   hDlg,   // дескриптор окна диалога
    _In_ UINT   uMsg,   // идентификатор сообщения (WM_*)
    _In_ WPARAM wParam, // первый параметр сообщения (зависит от uMsg)
    _In_ LPARAM lParam  // второй параметр сообщения
);

#endif