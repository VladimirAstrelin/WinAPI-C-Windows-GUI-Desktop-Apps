// ================================================================
// dialog_proc.h – прототип функции-обработчика диалога.
// Этот заголовок включается в main.c, чтобы точка входа
// знала, как вызывать нашу оконную процедуру.
// ================================================================

#ifndef DIALOG_PROC_H
#define DIALOG_PROC_H

// Прототип оконной процедуры.
// Параметры стандартные для любого диалога Windows.
// Возвращает INT_PTR (целое, совместимое с BOOL), TRUE – сообщение обработано.
INT_PTR CALLBACK TrayDialogProc(
    _In_ HWND   hDlg,    // дескриптор окна
    _In_ UINT   uMsg,    // идентификатор сообщения (WM_*)
    _In_ WPARAM wParam,  // первый параметр (зависит от сообщения)
    _In_ LPARAM lParam   // второй параметр
);

#endif // DIALOG_PROC_H