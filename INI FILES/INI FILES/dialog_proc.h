// ================================================================
// dialog_proc.h – заголовочный файл, объявляющий нашу оконную
// процедуру. Он включается в main.c, чтобы точка входа знала,
// какую функцию вызывать для обработки сообщений диалога.
// ================================================================

#ifndef DIALOG_PROC_H
#define DIALOG_PROC_H

// Прототип оконной процедуры.
// Параметры стандартные для диалогов Windows.
// _In_ – аннотация SAL, означает, что параметр является входным.
// CALLBACK – соглашение о вызове для callback-функций.
// Возвращает INT_PTR (целое, совместимое с BOOL), TRUE – сообщение обработано.
INT_PTR CALLBACK MainDialogProc(
    _In_ HWND   hDlg,    // дескриптор окна диалога
    _In_ UINT   uMsg,    // идентификатор сообщения (WM_*)
    _In_ WPARAM wParam,  // первый параметр (зависит от uMsg)
    _In_ LPARAM lParam   // второй параметр
);

#endif // DIALOG_PROC_H