// ================================================================
// dialog_proc.h – заголовочный файл, в котором объявлены процедуры
// обработки сообщений для главного диалога и для дочерних страниц.
// Этот файл включается в main.c и в dialog_proc.c.
// ================================================================

#pragma once                      // Защита от повторного включения

#include <windows.h>              // Нужен для типов HWND, UINT, WPARAM, LPARAM

// Процедура для главного диалога (содержит Tab Control).
// Обрабатывает все сообщения, связанные с основным окном.
INT_PTR CALLBACK MainDialogProc(HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM lParam);

// Процедура для дочерних диалогов (страниц вкладок).
// Обрабатывает сообщения от элементов управления, расположенных на конкретной вкладке.
// Например, нажатие кнопки "Show" на первой вкладке.
INT_PTR CALLBACK TabChildProc(HWND hwnd, UINT uMsg, WPARAM wParam, LPARAM lParam);